snapshots:
  - name: campaign_status_snapshot
    relation: source('raw_marketing', 'campaign_status')

    config:
      schema: snapshots
      unique_key: campaign_id
      strategy: timestamp
      updated_at: updated_at